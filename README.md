# Overlai

### 買う前に、家の棚を重ねて見る。

「その商品がいいかどうか」ではなく、**「あなたの家に、もうあるかどうか」**を判定する。

店頭の商品パッケージにカメラをかざすと、自宅の在庫データと突合して3色で判定します。

| | 意味 |
| :---: | :--- |
| 🔵 | 手持ちにない有用成分、または目的別の買い分けとして妥当 |
| 🟡 | 同一成分・同効能が自宅に存在。買わなくていい |
| 🔴 | 手持ちの処方薬・常用品との併用でリスクや強い刺激 |

---

## ドキュメント

| ドキュメント | 内容 |
| :--- | :--- |
| [技術ドキュメント](docs/ARCHITECTURE.md) | **実装されているもの**。アーキテクチャ・AIパイプライン・API仕様・意思決定の記録 |
| [設計・仕様書](docs/設計仕様書_Overlai_デモ版.md) | **何を作るかの合意文書**。要求定義・要件定義・受け入れ基準 |
| [企画書](docs/企画書_Overlai_ユメカタリ2026.md) | 課題・競合分析・社会的インパクト・ロードマップ |
| [要件定義（初版）](docs/要件定義_Overlai_デモ版.md) | 設計仕様書の前身。内容は設計仕様書に統合済み |
| [コンテスト概要](docs/CONTEST.md) | 応募先の評価軸と開発方針 |

**実装がどうなっているかを知りたい場合は技術ドキュメント、何を作るべきかの根拠は設計仕様書を参照する。**
各Issueには対応する章番号を記載している。

## 技術構成

| 層 | 選択 |
| :--- | :--- |
| フロント | Next.js (App Router) + TypeScript + Tailwind CSS |
| AI | Anthropic API (`claude-opus-5`) Vision + 構造化出力 |
| データ | シードJSON + localStorage（DB不使用） |
| デプロイ | Vercel |

### 設計上の要点

- **2段階のAIパイプライン**（成分抽出 → 在庫照合判定）。単一プロンプトにしない理由は仕様書 §7.2
- **JANコードではなく成分表示を直接読む**。化粧品の公式成分DBが日本に存在しないため（§7.1）
- **APIキーはサーバー側の Route Handler のみで保持**（§10）
- **判定理由に必ず成分名を含める**ことをスキーマで強制（§7.4・§12）

## 開発の進め方

Issue は仕様書 §14 の作業計画に対応し、**下から切り落とせる順**に並んでいる。

```
#1 初期化 → #2 型・シード → #3 判定カードUI → #4 マイストック
                              ↓
                     #5 判定API → #6 成分抽出 → #7 画像アップロード → #10 デプロイ
```

| 優先度 | Issue | 位置づけ |
| :--- | :--- | :--- |
| `must` | #1 #2 #3 #4 #5 #6 #7 #10 | **これが揃えばデモは成立する**（クリティカルパス・約10時間） |
| `should` | #8 #9 | 加点。落としてもデモは撮れる |
| `low` | #11 #12 | 時間がなければ落とす |

**#3（判定カードUI）を早期に着手する。** ダミーデータで表示できた時点でデモ映像の山場は撮れるため、以降のタスクが遅れても最低限が確保できる。

## セットアップ

```bash
npm install
npm run dev
```

### モックモードについて

**`ANTHROPIC_API_KEY` が未設定のとき、`/api/analyze` は固定の判定結果を返す**（`lib/mock.ts`）。
AIは呼ばれていない。UIの確認とデモ撮影のための経路である。

判定シナリオはクエリパラメータで選ぶ。

| URL | 判定 |
| :--- | :--- |
| `/?demo=yellow` | 🟡 買わなくて大丈夫です（イブプロフェン重複） |
| `/?demo=red` | 🔴 注意が必要です（ステロイド外用中 × エタノール／サリチル酸） |
| `/?demo=blue` | 🔵 買っても問題なさそうです（シャンプーの用途違い） |

指定はスキャン画面へ引き継がれるため、撮影の流れは通常どおり（マイストック → スキャン → 判定）で撮れる。
`/preview` では3色のカードを直接切り替えて確認できる。

### 本物のAIに切り替える

```bash
echo 'ANTHROPIC_API_KEY=sk-ant-...' > .env.local
```

**これだけでよい。コード変更は不要**（`app/api/analyze/route.ts` が自動的に本物のパイプラインに切り替わる）。
逆に、キーを設定したうえでモックに戻したい場合は `OVERLAI_MOCK=1` を設定する。

## ライセンス

未定（ユメカタリ学生生成AIコンテスト2026 応募作品）
