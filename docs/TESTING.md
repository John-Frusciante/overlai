# 提出前テスト手順

**私（AI）が確認できないもの＝実機・実物・人間の判断が必要なもの**だけを並べてある。
コード側の自動確認（型・ビルド・API疎通・3色判定）はすべて完了している（[STATUS.md](STATUS.md)）。

公開URL: **https://overlai-delta.vercel.app**

---

## A. 実物の商品での読み取りテスト ✅完了（2026年9月9日）

> **完了済み。** 実物5商品で全パターンを確認した。
>
> | 商品 | 判定 |
> | :--- | :---: |
> | バファリン プレミアムDX クイックプラス | 🟡 |
> | ストナジェルサイナスEX（総合感冒薬） | 🔴 |
> | 肌ラボ 極潤 薬用トラブルケア | 🔴（外用薬 × 化粧品） |
> | 肌ラボ 極潤 ヒアルロン液 | 🔵 |
> | 御岳百草丸 | 🔵 |
>
> この検証で見落とし（抗ヒスタミン重複）と過剰警告（防腐剤）の両方を発見・修正した。
> 別の商品を試す場合は以下の手順で。

これまでのテストはHTMLから生成した綺麗な成分表示画像で行っており、実物のパッケージ（光沢・曲面・小さい文字・照明のムラ）では結果が変わりうる。

### 用意するもの
- 手持ちの**市販薬**を1つ（鎮痛薬・胃薬など何でも）
- 手持ちの**化粧水か乳液**を1つ

### 手順
1. スマホで https://overlai-delta.vercel.app/ を開く
2. 下部中央のスキャンボタン → カメラが起動
3. **パッケージの成分表示**（「成分・分量」「全成分」の欄）を枠に入れて撮影
4. 判定が出るまで待つ（9〜11秒）
5. **「根拠を見る」を開いて、読み取られた成分名が正しいか確認する**

### 確認すること

| 項目 | 見るところ |
| :--- | :--- |
| 成分名が正確か | 判定カード上部の成分タグ。誤字（「いブプロフェン」など）が無いか |
| 商品名が合っているか | 成分タグの上 |
| 判定が納得できるか | 色・見出し・要約 |
| 所要時間 | カード下部に「判定にかかった時間 ◯秒」が出る |

### 失敗したときの対処

| 症状 | 対処 |
| :--- | :--- |
| 「成分表示を読み取れませんでした」（422） | 明るい場所で、成分表示に**寄って**撮り直す。影と光の反射を避ける |
| 成分名が一部間違っている | 別の角度・距離で再撮影。それでもダメなら、その商品はデモに使わない |
| ボトルの曲面で読めない | ラベルの平らな部分を正面から。箱があれば箱の成分表示を撮る |
| どうしても通らない | `?demo=` のモックに切り替えて撮影する（§D） |

**デモ動画には、ここで確実に読み取れた商品だけを使うこと。**

---

## B. 実機でのUI確認（5分）

1. **3画面を開く** — マイストック（`/`）／ルーティン（`/routine`）／スキャン
2. **表示崩れがないか** — 文字が切れていないか、ボタンが押しにくくないか
3. **PWA** — ブラウザの共有メニューから「ホーム画面に追加」→ アイコンから起動
   - アドレスバーが消えて全画面になるか
   - 下部のホームインジケーターにボタンが重なっていないか
4. **肌質設定** — `/profile` で頭皮を「脂性」にしてから `/routine` を見る
   → シャンプーの相性コメントが変わるはず

---

## C. デモの通しリハーサル（15分）

**本番と同じ流れを、録画せずに一度通す。**

```
マイストック（家にあるものが並んでいる）
   ↓ スキャンボタン
カメラ起動 → 成分表示を撮影
   ↓ 「成分を読み取っています」→「家の在庫と照合しています」
判定カード（色・見出し・要約）
   ↓ 「根拠を見る」
判定理由（成分名つき）
```

- **🟡と🔴の2本**を通す（企画の半分ずつを担っているため）
- 待ち時間（9〜11秒）が思ったより長く感じるかを体感しておく
- 通し終わったら、**同じ流れで1本録画してみる**（本番前に必ず1本撮っておく）

### 在庫が判定を変えることを見せるカット

**このアプリの主張が一番はっきり映る流れ。** 商品側のDBで戦う既存アプリには原理的にできない。

```
百草丸をスキャン         → 🔵 買っても問題なさそう
   ↓ ストックを追加
百草丸をスキャン（同じ商品）→ 🟡 家にあります
   ↓ 百草丸をタップ → 削除する → 確認 → 削除する
百草丸をスキャン（同じ商品）→ 🔵 に戻る
```

商品は一度も変えていない。**変えたのは家の在庫だけ。**

### B. の UI 確認に足すこと

- マイストックで**カテゴリ見出しをタップ**して開閉する（品名の要約が残ること）
- スキャン画面へ行って戻り、**閉じた状態が保たれている**こと
- **項目をタップ**して操作シートが上がること。「削除する」→ 確認 →「やめる」で消えないこと
- 編集モード（ヘッダーの「編集」）でも同じ操作ができ、タップの導線と競合しないこと

---

## D. 障害時のフォールバック確認（10分）

逃げ道は2段ある。**AI経路の切り替え**（自動）と、**デモ用の固定応答**（手動）。

### D-1. プロバイダのフォールバック

学校配布のプロキシが止まったとき、Gemini に落ちて処理が続くか。本番では Azure が生きている限り Gemini は呼ばれないので、**ローカルでキーを壊して確認する**。

```bash
# Azure を無効にして Gemini だけで起動する
AZURE_PROXY_KEY= npm run dev

# 成分表示の画像を送る（実物の写真を base64 にして payload.json に入れておく）
curl -s -X POST http://localhost:3000/api/analyze \
  -H 'Content-Type: application/json' --data-binary @payload.json
```

`provider` が `gemini` になり、抽出と判定が両方返れば成功（実測 13.9〜30秒）。

チェーンが順に落ちること自体を見るなら、**両方を無効なキーにして**ログを読む。

```bash
AZURE_PROXY_KEY=invalid GEMINI_API_KEY=invalid npm run dev
```

```
[extract] azure 失敗 (auth) Error: 403 ...
[extract] gemini 失敗 (auth) Error [ApiError]: {"error":{"code":400,...
[analyze] 認証エラー: APIキーを確認してください
```

この順に出れば、チェーンは意図どおり回っている。

> **注意**: Gemini の無料枠は 5 RPM。`curl --retry` を付けるとHTTPエラーにもリトライがかかり、数回で枠を使い切る。**リトライは付けずに1回ずつ叩くこと。**

### D-2. デモ用の固定応答

デモ当日にどのAIも使えない場合の最後の逃げ道。

1. `https://overlai-delta.vercel.app/?demo=red` を開く
2. スキャン → 何を撮っても🔴が出る（AIを呼ばず固定応答が返る）
3. `?demo=yellow` `?demo=blue` も同様

**この指定はマイストックからスキャン画面へ引き継がれる**ので、撮影の流れは通常どおりに映る。

また `/preview?open=red` で判定カードだけを直接開ける。

---

## E. 判定内容の妥当性チェック（人間の目で・10分）

**AIの出力が医学的・常識的に妥当か**は、私には保証できない。当事者の目で見てほしい。

- [ ] 事実と違うことを言っていないか
- [ ] 「〜です」と**断定していないか**（「〜の可能性があります」になっているか）
- [ ] 診断らしきこと（病名の推定、用法用量の指示）を言っていないか
- [ ] 根拠に**成分名が入っているか**
- [ ] 過剰に警告していないか（何を撮っても🔴になるようなら要調整）

**おかしな出力を見つけたら、その画面のスクリーンショットを共有してほしい。** プロンプトを直す。

---

## F. 先生への確認（5分）

- **プロキシの利用枠とレート制限** — デモ中に上限に当たらないか
- **有効期限** — 本選は **2026年10月1日（名古屋）／10月11日（東京）**。それまで使えるか
- コンテスト作品で使うことに問題がないか

万一止まってもモック（§D）で動くが、その場合「AIが判定している」とは言えなくなる。

---

## G. ルーティン機能の確認（5分）

順序はルール、言葉はAIという切り分けが崩れていないかを見る。

### G-1. 服薬設定

1. `/stock/new` で剤形に「錠剤」を選ぶ → **服薬の欄が現れる**
2. 飲むタイミングに「朝」「夜」、1回量に 1、残量に 28 を入れて保存
3. `/routine` の「今日のお薬」に並ぶ。**朝と夜のボタンだけが出る**（昼は出ない）
4. 「朝」をタップ → 残薬が 27 に減る。もう一度タップすると 28 に戻る
5. 一覧からその項目をタップ → 編集 → **入力した値が初期値として入っている**
6. 剤形を「化粧水」に変えて保存 → 服薬の欄が消え、「今日のお薬」からも外れる

服薬設定が1件も無い状態（`/` の「編集 → 初期化」の前後）でも、見出しと設定への導線が出ることを確認する。

### G-2. 肌質・頭皮の反映

1. `/profile` で頭皮を「脂性」に設定 → シャンプーの下のコメントが「合いやすい傾向」側に変わる
2. 「乾燥」に変える → 同じシャンプーで「刺激になる可能性」側に変わる
3. **「普通」でもコメントが出る**（以前は系統の一般説明に落ちていた）
4. 石鹸系のシャンプー（`石ケン素地` を含むもの）を登録しても頭皮の設定が効く

### G-3. AIの一言

1. `/routine` を開く → 「今日の組み合わせについて」にスケルトンが出て、数秒後に文章が入る
2. **リロードする → 即座に表示される**（ネットワークタブに `/api/routine` が出ないこと）
3. 「朝」のチェックを付け外しする → **再生成されない**（残量は署名に含めない）
4. ストックを1件追加する → 次に `/routine` を開いたときに作り直される
5. `.env.local` のキーを壊して開く → **一言だけが出ず、順序と理由は普通に表示される**

5 は「解説が落ちても画面は成立する」ことの確認。エラー表示は出さない仕様なので、出ていたら実装がずれている。

### G-4. 手動並べ替え

1. 「お風呂上がりに塗る順番」の「並べ替え」を押す → 各ステップに ↑↓ が出る
2. 軟膏を1つ上へ動かす → 番号が振り直され、見出しの説明が「自分で並べた順です」に変わる
3. リロードしても並びが保たれる
4. 新しくクリームを登録する → **手動の並びに割り込まず、末尾に追加される**
5. 「剤形どおりの順に戻す」→ 元の並びと説明に戻る
6. 並べ替えた直後に `/routine` を開き直すと、**AIの一言が作り直される**（並び順は署名に含まれる）

その項目の剤形を変えて「お風呂で洗う」に移すと、移した先で先頭に割り込まないことも確認する。

> **リトライは付けずに1回ずつ叩くこと。** `curl --retry` は 500 応答にも再試行するため、無料枠を一瞬で使い切る。

---

## 完了したもの（再確認は不要）

| 項目 | 結果 |
| :--- | :--- |
| 型チェック・本番ビルド | 通過 |
| 全7ページの疎通（本番） | 200 |
| 🟡🔴🔵＋吸収阻害の4シナリオ（本番・実AI） | すべて期待通り |
| **実物5商品での判定** | 🟡🔴🔴🔵🔵 すべて妥当 |
| **判定の一貫性**（同じ画像を3回ずつ×3シナリオ） | 9回すべて同じ結果。揺らぎなし |
| **在庫追加 → 判定への反映**（実機） | 百草丸を追加すると同じ商品の判定が🔵→🟡に変わることを確認 |
| **エラー系**（422 / 400 / 在庫空） | 適切なコードとメッセージ、導線を確認 |
| カメラ実機起動 | 確認済み |
| PWAマニフェスト・アイコン配信 | 確認済み |
| ハイドレーションエラー | 0件 |
| **在庫の削除**（WebKit 390×844） | タップ導線・編集モードの両方で 11件→10件。「やめる」では変化しない |
| **操作シート** | 背景タップ・`×`・Escape で閉じる。削除後は自動で閉じる |
| **在庫の編集**（`/stock/new?id=`） | 名前・カテゴリ・剤形・成分・状態・開封日の6項目が初期値として入る |
| **カテゴリの折りたたみ** | 開閉がリロードをまたいで保たれる（`overlai.collapsed.v1`） |

## 残っている確認

**デモ動画の撮影のみ。** 品質に関わる確認はすべて完了した。

| 項目 | 状態 |
| :--- | :--- |
| PWA（ホーム画面追加 → 全画面起動） | ✅ 実機で確認 |
| Android Chrome での表示 | ✅ 実機で確認 |
| 在庫を追加してから判定 | ✅ 実機で確認（🔵→🟡） |
| プロキシの利用条件 | ✅ 先生より無条件で利用可の回答 |
| Anthropic Claude 経路 | キー入手時に確認（[#20](https://github.com/John-Frusciante/overlai/issues/20)） |
| Google Gemini 経路 | ✅ ローカルで確認（抽出→判定が13.9秒で完走。成分名の誤読なし） |
| プロバイダのフォールバック | ✅ ローカルで確認（azure → gemini → 例外の順に落ちる） |
| 本番での Gemini 発火 | **確認不可** — Azure が動く限り呼ばれない作りのため |
| ルーティンの解説（`/api/routine`） | ✅ ローカルで確認（Azure 経路 6.5秒。肌質・頭皮が文面に反映される） |
| 解説のキャッシュが効いているか | 未確認（§G-3。実機で見たい） |
| 服薬設定のUI | 未確認（§G-1。実機で見たい） |
| ルーティンの手動並べ替え | 未確認（§G-4。実機で見たい） |

### 本選に進んだ場合に足すテスト

ブース展示は**来場者が実機を触る**点と**会場のネットワークを使う**点が、動画撮影と決定的に違う。

| 項目 | Issue |
| :--- | :---: |
| 429・タイムアウト・オフラインを意図的に起こしてUIを確認する | [#23](https://github.com/John-Frusciante/overlai/issues/23) |
| 連続して何人も触ったあと、初期状態に戻せるか | [#25](https://github.com/John-Frusciante/overlai/issues/25) |
| 数週間空けたあとに在庫が残っているか（Safari の ITP） | [#22](https://github.com/John-Frusciante/overlai/issues/22) |
